//rsc

import React from "react";
import styled from "styled-components";

const Header = () => {
  return <HeaderText>할 일</HeaderText>;
};

const HeaderText = styled.h1`
  color: red;
`;

export default Header;
